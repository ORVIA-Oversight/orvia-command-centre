import { Shell } from '@/components/Shell'; import { Topbar } from '@/components/Topbar'; import { integrations } from '@/lib/data'; import { StatusBadge } from '@/components/StatusBadge';
export default function Page(){return <Shell><Topbar title="Systems & Telemetry"/><div className="pageWrap"><section className="pageIntro"><div className="eyebrow">VISIBILITY · ACCOUNTABILITY</div><h2>One operating picture for the ORVIA digital estate.</h2><p>Connection state, webhook health, background ingestion and operational events are presented with explicit evidence labels.</p></section><section className="panel"><div className="panelHead"><div><span>CURRENT CONNECTION MAP</span><h3>Systems</h3></div></div><div className="panelBody systemsGrid">{integrations.map(x=><div className="systemRow" key={x.name}><div><b>{x.name}</b><small>{x.category} · {x.note}</small></div><StatusBadge status={x.state}/></div>)}</div></section><section className="twoCol spaced"><article className="panel"><div className="panelHead"><div><span>WEBHOOKS</span><h3>Ingestion endpoint</h3></div></div><div className="panelBody"><code className="codeBlock">POST /api/ingest<br/>Authorization: Bearer &lt;INGEST_API_KEY&gt;</code><p className="mutedText">Use from Make, Zapier or approved Python scrapers. The endpoint stores telemetry events only when the live Supabase environment is configured.</p></div></article><article className="panel"><div className="panelHead"><div><span>PRIVACY</span><h3>Review access</h3></div></div><div className="panelBody"><p className="mutedText">Set COMMAND_PRIVATE_MODE=true plus username/password in Vercel environment variables to put the whole site behind HTTP Basic Auth. Never commit credentials to GitHub.</p></div></article></section></div></Shell>}
